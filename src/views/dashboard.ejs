<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title>Dashboard</title>
    <style>
        body {
            margin: 0; /* Remove default margin */
            overflow: hidden; /* Prevent scrolling */
        }

        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('/college.jpg'); /* Path to your background image */
            background-size: cover;
            background-position: center;
            filter: blur(8px); /* Apply blur effect */
            z-index: -1; /* Ensure the background is behind the content */
            opacity: 0.6; /* Slight transparency for a lively effect */
        }

        /* Container for the cards */
        .container {
            display: flex;
            flex-wrap: wrap; /* Allow wrapping */
            justify-content: center; /* Center cards */
            align-items: center; /* Center vertically */
            height: 100vh; /* Full viewport height */
            gap: 20px; /* Space between cards */
            padding: 20px; /* Padding around the container */
            box-sizing: border-box; /* Include padding in width calculations */
        }

        /* Card styles */
        .card {
            background-color: #f0f0f0; /* Initial card background color */
            border-radius: 10px; /* Rounded corners */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Shadow effect */
            width: 150px; /* Width of the card */
            height: 150px; /* Height of the card (making it square) */
            text-align: center; /* Center text */
            padding: 10px; /* Padding inside the card */
            transition: transform 0.3s, box-shadow 0.3s, background-color 0.3s; /* Transition effects */
            cursor: pointer; /* Pointer cursor */
            position: relative; /* Position for the image */
        }

        /* Card hover effect */
        .card:hover {
            transform: translateY(-5px); /* Move card up */
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Enhance shadow on hover */
            background-color: #4e54c8; /* Change background color to match heading */
            color: white; /* Change text color to white */
        }

        /* Bus image style */
        .card img {
            width: 80px; /* Adjust image width */
            height: auto; /* Maintain aspect ratio */
            position: absolute; /* Position the image */
            top: 10px; /* Position from the top */
            left: 50%; /* Center the image */
            transform: translateX(-50%); /* Center the image */
        }

        /* Heading inside the card */
        .card h2 {
            margin-top: 70px; /* Space between image and heading */
            font-size: 14px; /* Font size for the heading */
        }

        /* Responsive styles for smaller screens */
        @media (max-width: 600px) {
            .card {
                width: 120px; /* Adjust width for smaller screens */
                height: 120px; /* Adjust height for smaller screens */
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 style="width: 100%; text-align: center; color:#4e54c8;">Welcome to the Dashboard</h1>
        <div class="card">
            <img src="/bus.png" alt="Bus Image">
            <h2>Bus 1</h2>
        </div>
        <div class="card">
            <img src="/bus.png" alt="Bus Image">
            <h2>Bus 2</h2>
        </div>
        <div class="card">
            <img src="/bus.png" alt="Bus Image">
            <h2>Bus 3</h2>
        </div>
        <div class="card">
            <img src="/bus.png" alt="Bus Image">
            <h2>Bus 4</h2>
        </div>
        <div class="card">
            <img src="/bus.png" alt="Bus Image">
            <h2>Bus 5</h2>
        </div>
        <div class="card">
            <img src="/bus.png" alt="Bus Image">
            <h2>Bus 6</h2>
        </div>
        
    </div>
</body>
</html>
